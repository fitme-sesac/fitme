package com.example.pproject.Constant;

/**
 * 면접 일정 상태
 */
public enum InterviewStatus {
    PROPOSED,   // 제안됨 (기업이 일정 제안)
    CONFIRMED,  // 확정됨 (지원자가 수락)
    CANCELED,   // 취소됨
    DONE        // 완료됨
}
