spring:
  application:
    name: PProject

  # PostgreSQL 설정
  datasource:
    url: "jdbc:postgresql://localhost:5432/fitme-project"
    username: postgres
    password: 01dbsrud
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  # H2 인메모리 데이터베이스 설정 (MySQL 없이 실행 가능)
  # datasource:
  #   url: "jdbc:h2:mem:testdb"
  #   driver-class-name: org.h2.Driver
  #   username: sa
  #   password: ""
  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console
  # jpa:
  #   hibernate:
  #     ddl-auto: create-drop
  #   database-platform: org.hibernate.dialect.H2Dialect

  # MySQL 설정 (MySQL이 설치되어 있을 때 사용하려면 아래 주석을 해제하고 위의 PostgreSQL 설정을 주석 처리)
  # datasource:
  #   url: "jdbc:mysql://127.0.0.1:3306/pproject?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8"
  #   username: rhtkdwls
  #   password: "rhtkdwls!2"
  #   driver-class-name: com.mysql.cj.jdbc.Driver
  # jpa:
  #   hibernate:
  #     ddl-auto: update
  #   database-platform: org.hibernate.dialect.MySQLDialect

  thymeleaf:
    cache: false
    check-template-location: true
    prefix: classpath:/templates/
    suffix: .html
    encoding: utf-8

  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 50MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: h321970921@gmail.com
    password: "uasq ynme umkz ivqz"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: "1031252790504-2u4c7fnqjo9fgugid93r2ffphr3p4dv1.apps.googleusercontent.com"
            client-secret: "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"
            scope:
              - email
              - profile
            redirect-uri: "http://localhost:8080/login/oauth2/code/google"
            client-name: Google

server:
  port: 8080
  forward-headers-strategy: framework
  error:
    whitelabel:
      enabled: false

# Local/AWS upload path (keep as top-level custom keys as in properties)
tempFolder: "C:/PProject/uploads/"
uploadPath: "file:///C:/PProject/uploads/"

jwt:
  secret: "ThisIsVeryLongJwtSecretKeyForPProject123456"
  access-token-validity-seconds: 7200

imgUploadLocation: item

oauth2:
  auth-request-cookie:
    name: OAUTH2_AUTH_REQUEST
    ttl-seconds: 300
    secret: "ChangeMeTo32CharsMin_ReplaceInProd_123456"

logging:
  level:
    org.springframework.security: DEBUG

app:
  front-base-url: "http://localhost:5173"
